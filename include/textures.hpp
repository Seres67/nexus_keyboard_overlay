#ifndef NEXUS_KEYBOARD_OVERLAY_TEXTURES_HPP
#define NEXUS_KEYBOARD_OVERLAY_TEXTURES_HPP

/** Workflow:
    - check if texture exists (path)
    - create it from file
    - show preview
    - if save:
        - move it to addons/keyboard_overlay/textures
        - rename it to <vk>_<released/pressed>
    - delete old texture
    - create from file
*/
#endif // NEXUS_KEYBOARD_OVERLAY_TEXTURES_HPP
