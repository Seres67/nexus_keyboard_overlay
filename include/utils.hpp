#ifndef NEXUS_KEYBOARD_OVERLAY_UTILS_HPP
#define NEXUS_KEYBOARD_OVERLAY_UTILS_HPP

#include <nexus/Nexus.h>
#include <string>

std::string key_to_string(UINT virtual_key, UINT scan_code);

#endif // NEXUS_KEYBOARD_OVERLAY_UTILS_HPP
